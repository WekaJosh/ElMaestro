# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Build artifacts (anchored so they don't accidentally shadow a Python
# module with the same name; cf. the earlier /results/ rule)
/build/
/dist/

# Virtualenv / uv
.venv/
venv/
.python-version

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Local results from runs (anchored to repo root so it doesn't shadow the
# src/elbencho_harness/results/ Python module)
/results/
/results-smoke/
*.log
